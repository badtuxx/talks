pipeline {
    agent any

    stages {
        stage ("Cloning repo") {
            checkout scm
        }
    }
        stage ("Testing - UT IT") {

        }
        stage ("Build") {
            sh "/bin/cat ./talks/LICENSE"
        }
}
